# Titanic — Machine Learning from Disaster
case_id: titanic
kaggle_competition: titanic
problem_statement: |
  Predict which passengers survived the 1912 sinking of the Titanic.
  Train a model on a small set of labelled passenger records and produce
  predictions for the held-out test set.

problem_type: binary_classification
target_column: Survived
id_column: PassengerId
evaluation_metric: accuracy   # matches Kaggle's metric

# Where the raw data lives, relative to repo root, after download.
data:
  train_csv: data/titanic/train.csv
  test_csv: data/titanic/test.csv
  sample_submission_csv: data/titanic/gender_submission.csv

# Hints the Domain Expert may use (do not hardcode in agent code).
feature_hints:
  numeric_with_missing: [Age, Fare]
  categorical: [Pclass, Sex, Embarked]
  ordinal: []
  text_free: [Name, Ticket, Cabin]
  high_missing: [Cabin]

class_labels:
  "0": "Not survived"
  "1": "Survived"

# Business success criterion — used by Evaluation phase.
success_criterion:
  metric: accuracy
  threshold: 0.77   # below this, the 5 -> 1 loop should fire
