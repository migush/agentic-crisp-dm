state_only:
  description: 'Current CRISP-DM state (JSON):

    {state_view}


    {instruction}


    Respond ONLY with JSON matching the target schema.'
  expected_output: A single JSON object, no prose, no Markdown fences. Response must begin with '{' and end with '}'.
substep_json:
  description: 'CRISP-DM substep {substep} ({substep_name}).

    {instruction}


    Relevant state (JSON):

    {state_view}


    Respond ONLY with JSON matching: {schema_hint}'
  expected_output: A single JSON object, no prose, no Markdown fences. Response must begin with '{' and end with '}'.
authored_code:
  description: '{instruction}


    Relevant state (JSON):

    {state_view}


    Return a Python code block only. Do not wrap the code in JSON.'
  expected_output: A Python code block.
