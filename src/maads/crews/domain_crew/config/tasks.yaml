# Scaffold stubs only — live task descriptions come from compile_task_payload
# (src/maads/config/tasks.yaml) via kickoff_json / run_json_task.
task_1_1:
  description: "CRISP-DM 1.1 — description assembled at kickoff via compile_task_payload."
  expected_output: A single JSON object, no prose, no Markdown fences. Response must begin with '{' and end with '}'.
  agent: domain

task_1_2:
  description: "CRISP-DM 1.2 — description assembled at kickoff via compile_task_payload."
  expected_output: A single JSON object, no prose, no Markdown fences. Response must begin with '{' and end with '}'.
  agent: domain

task_1_3:
  description: "CRISP-DM 1.3 — description assembled at kickoff via compile_task_payload."
  expected_output: A single JSON object, no prose, no Markdown fences. Response must begin with '{' and end with '}'.
  agent: domain
